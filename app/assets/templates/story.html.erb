<div >
  <h1>{{ currentStory.title }}</h1>

  </br>

  <div>
    {{ currentPage.content }}
  </div>

  <ol>
    <li ng-repeat='choice in currentPage.choices'>
      <span ng-click='setPage(choice.dest)'> {{choice.text}} </span>
    </li>
  </ol>
